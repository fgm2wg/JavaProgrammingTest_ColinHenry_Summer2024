package org.example.Q5_GCD;

public class GCDRunner {
    /**
     * Demonstrates concurrency by running the gcd calculation for each integer pair on a separate thread via a lambda body
     */
    public static void run() throws InterruptedException {
        System.out.println("--- Question 5. Greatest Common Divisor ---");

        CSVReader csvReader = new CSVReader();
        csvReader.readLines();
    }
}